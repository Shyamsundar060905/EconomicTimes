# Copy to .env for local work. NEVER commit .env — add real keys in GitHub
# repo Settings -> Secrets for CI. All keys are free-tier.
OPENAQ_API_KEY=            # https://explore.openaq.org/register
FIRMS_KEY=                 # https://firms.modaps.eosdis.nasa.gov/api/area/
GEMINI_API_KEY=            # LLM explanations (optional; rules fallback without it)
GROQ_API_KEY=              # LLM fallback provider (optional)
# GEE uses a service-account JSON, not an env key: see docs (pending setup)
